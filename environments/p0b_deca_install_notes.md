# P0-B DECA isolated environment

Create a dedicated environment only after inspecting `third_party/DECA/requirements.*`,
the DECA commit, the NVIDIA driver, and an installable PyTorch/PyTorch3D pairing.
Protected DECA/FLAME assets must be supplied by the user under their applicable licence;
the audit script does not download or bypass them.
