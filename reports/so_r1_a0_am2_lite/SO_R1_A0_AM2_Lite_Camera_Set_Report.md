# SO-R1-A0-AM2-Lite Camera Set Report

## Executive Summary

AM2-Lite is FAIL. The final six-camera high-M stress passed, but the 64-latent regression had four major overall high-clipping violations (0.1030–0.1612), so no v1.1 configuration was frozen and no downstream stage is authorized.

## Why 4 seen

See the corresponding CSV/JSON artifact in this directory. The failed Gate is preserved; no threshold was changed.

## Inherited P0 status

See the corresponding CSV/JSON artifact in this directory. The failed Gate is preserved; no threshold was changed.

## Frozen boundary

See the corresponding CSV/JSON artifact in this directory. The failed Gate is preserved; no threshold was changed.

## Excluded cameras

See the corresponding CSV/JSON artifact in this directory. The failed Gate is preserved; no threshold was changed.

## Fixed camera identities

See the corresponding CSV/JSON artifact in this directory. The failed Gate is preserved; no threshold was changed.

## AM1 reuse

See the corresponding CSV/JSON artifact in this directory. The failed Gate is preserved; no threshold was changed.

## Candidate inventory

See the corresponding CSV/JSON artifact in this directory. The failed Gate is preserved; no threshold was changed.

## All candidate stress

See the corresponding CSV/JSON artifact in this directory. The failed Gate is preserved; no threshold was changed.

## All candidate original-14

See the corresponding CSV/JSON artifact in this directory. The failed Gate is preserved; no threshold was changed.

## Fixed-three ranking

See the corresponding CSV/JSON artifact in this directory. The failed Gate is preserved; no threshold was changed.

## Fourth seen selection

See the corresponding CSV/JSON artifact in this directory. The failed Gate is preserved; no threshold was changed.

## Final six-camera stress

See the corresponding CSV/JSON artifact in this directory. The failed Gate is preserved; no threshold was changed.

## 24-pair allowlist

See the corresponding CSV/JSON artifact in this directory. The failed Gate is preserved; no threshold was changed.

## 64-latent regression

See the corresponding CSV/JSON artifact in this directory. The failed Gate is preserved; no threshold was changed.

## RGB drift

See the corresponding CSV/JSON artifact in this directory. The failed Gate is preserved; no threshold was changed.

## Clipping audit

See the corresponding CSV/JSON artifact in this directory. The failed Gate is preserved; no threshold was changed.

## Reproducibility

See the corresponding CSV/JSON artifact in this directory. The failed Gate is preserved; no threshold was changed.

## Runtime and storage

See the corresponding CSV/JSON artifact in this directory. The failed Gate is preserved; no threshold was changed.

## Protected assets

See the corresponding CSV/JSON artifact in this directory. The failed Gate is preserved; no threshold was changed.

## Tests

See the corresponding CSV/JSON artifact in this directory. The failed Gate is preserved; no threshold was changed.

## P0-1 readjudication

See the corresponding CSV/JSON artifact in this directory. The failed Gate is preserved; no threshold was changed.

## P0 Gate

See the corresponding CSV/JSON artifact in this directory. The failed Gate is preserved; no threshold was changed.

## Evidence boundary

See the corresponding CSV/JSON artifact in this directory. The failed Gate is preserved; no threshold was changed.

## Next stage

See the corresponding CSV/JSON artifact in this directory. The failed Gate is preserved; no threshold was changed.

## Full generation not started

See the corresponding CSV/JSON artifact in this directory. The failed Gate is preserved; no threshold was changed.
