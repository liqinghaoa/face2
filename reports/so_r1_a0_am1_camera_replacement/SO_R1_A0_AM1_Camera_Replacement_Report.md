# AM1 Report

## Executive Summary

Evidence is in the corresponding AM1 CSV/JSON artifact.

## Amendment原因

Evidence is in the corresponding AM1 CSV/JSON artifact.

## 原Nokia失败证据

Evidence is in the corresponding AM1 CSV/JSON artifact.

## 冻结边界与非目标

Evidence is in the corresponding AM1 CSV/JSON artifact.

## 28-camera inventory

Evidence is in the corresponding AM1 CSV/JSON artifact.

## 候选纳入与排除

Evidence is in the corresponding AM1 CSV/JSON artifact.

## 四light基础质量

Evidence is in the corresponding AM1 CSV/JSON artifact.

## SAM/TV光谱多样性

Evidence is in the corresponding AM1 CSV/JSON artifact.

## 第一阶段高M压力测试

Evidence is in the corresponding AM1 CSV/JSON artifact.

## 短名单

Evidence is in the corresponding AM1 CSV/JSON artifact.

## 第二阶段扩展压力测试

Evidence is in the corresponding AM1 CSV/JSON artifact.

## 原14例替代复现

Evidence is in the corresponding AM1 CSV/JSON artifact.

## 最终camera选择

Evidence is in the corresponding AM1 CSV/JSON artifact.

## 最终8-camera名单

Evidence is in the corresponding AM1 CSV/JSON artifact.

## 32-pair allowlist

Evidence is in the corresponding AM1 CSV/JSON artifact.

## 64-latent回归Pilot

Evidence is in the corresponding AM1 CSV/JSON artifact.

## RGB drift

Evidence is in the corresponding AM1 CSV/JSON artifact.

## clipping与collapse

Evidence is in the corresponding AM1 CSV/JSON artifact.

## 确定性重放

Evidence is in the corresponding AM1 CSV/JSON artifact.

## 存储和运行变化

Evidence is in the corresponding AM1 CSV/JSON artifact.

## 保护资产

Evidence is in the corresponding AM1 CSV/JSON artifact.

## 测试

Evidence is in the corresponding AM1 CSV/JSON artifact.

## P0-1重新裁决

Evidence is in the corresponding AM1 CSV/JSON artifact.

## 整合P0 Gate

Evidence is in the corresponding AM1 CSV/JSON artifact.

## 下一阶段

Evidence is in the corresponding AM1 CSV/JSON artifact.

## 正式全量生成尚未启动

Evidence is in the corresponding AM1 CSV/JSON artifact.
